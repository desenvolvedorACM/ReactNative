import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center'
    },
    mapa: {
        width: '100%',
        height: 550
    },
    header: {
        height: 60,
        width: '100%',
        backgroundColor: '#8877EE',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 15
    }
});

export default styles;