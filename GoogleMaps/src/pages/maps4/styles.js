import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center'
    },
    mapa: {
        width: '100%',
        height: '100%'
    },
    botaoLocation: {
        width: '100%',
        height: 50,
        backgroundColor: '#5689FF',
        borderRadius: 15,
        alignItems: 'center',
        justifyContent: 'center'
    }
});

export default styles;